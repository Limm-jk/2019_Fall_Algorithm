
public class Kruskal {
	public void MST_Krustal(int[][] graph, vertex[] ver) {
		// a= null;
		for(int verIndex = 0; verIndex < ver.length; verIndex++ ) {
			//makeset
		}
		//소트 -> 뭘?
		for(int adjIndex = 0; adjIndex < ver.length; adjIndex++) {
			//파인드 셋이 같은지 비교
			//같지 않을 경우 a에 붙임
			//유니온
		}
		//리턴a
	}
}
